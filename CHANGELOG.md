* `tcl` now can toggle with `tcls` (in addition to `null`, `tcl` & `tc`).
* `put` now handles strings and falsy values.
* When pass array as `element`'s' content, now for it is possible to contain falsy values.

## `0.3.0` — 2020-01-07
